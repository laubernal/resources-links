import { Title } from '@mantine/core';
import React from 'react';

function ResourcesLinks(): JSX.Element {
  return <Title order={1}>Create your first resource</Title>;
}

export default ResourcesLinks;
